Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
Flask-Limiter==3.8.0
Flask-JWT-Extended==4.6.0
PyMySQL==1.1.1
cryptography==43.0.0
python-dotenv==1.0.1
email-validator==2.2.0

# Phase 29 (Gmail SMTP -> Resend): Railway blocks outbound SMTP
# (ports 465/587) on every plan below Pro, so app/email_utils.py sends
# through Resend's HTTPS API instead of smtplib — HTTPS on port 443 is
# never blocked. See app/email_utils.py's module docstring.
requests==2.32.3

pytest==8.3.3

cloudinary==1.41.0
gunicorn==22.0.0

# Phase 28 (signature scan cleanup): server-side image processing for
# upload_assignment_signature() in app/routes/api_v1/technician.py —
# turns a phone photo of a paper signature into a clean black-ink /
# transparent-background cutout. pillow-heif registers a Pillow opener
# for .heic/.heif (default camera format on many iPhones), which
# Pillow itself can't decode out of the box.
Pillow==10.4.0
pillow-heif==0.18.0